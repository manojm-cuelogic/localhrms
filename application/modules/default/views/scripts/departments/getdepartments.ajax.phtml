<?php 
	foreach ($this->deptmodeldata as $dept) {
	$this->positionsform->departmentid->addMultiOption($dept['id'],utf8_encode($dept['deptname']));
	}
	echo $this->positionsform->departmentid;
?> 
 